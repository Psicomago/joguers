<%= form_with(model: sport) do |form| %>
  <% if sport.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
        <% sport.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :sport_name, style: "display: block" %>
    <%= form.text_field :sport_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
